<!DOCTYPE html>
<html>
<head>
  <title>CSS Selectors (Simple)</title>
  <style>
    /* Universal Selector */
    * {
      font-family: Arial;
    }

    /* Type Selector */
    h1 {
      color: blue;
    }

    /* Class Selector */
    .highlight {
      color: green;
    }

    /* ID Selector */
    #special {
      color: red;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <h1>Welcome to CSS Selectors</h1>
  <p class="highlight">This is styled with a class selector.</p>
  <p id="special">This is styled with an ID selector.</p>

</body>
</html>
